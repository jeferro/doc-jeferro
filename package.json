{
  "name": "doc-jeferro",
  "version": "1.0.0",
  "description": "JF Team documentation",
  "scripts": {
    "build": "antora site.yml",
    "start": "live-server build/site --port=5000 & run-when-changed --watch 'docs/**' --watch 'ui-bundle/**' --watch 'extensions/**' --watch 'site.yml' --exec 'npm run build'"
  },
  "devDependencies": {
    "antora": "^3.0.2",
    "live-server": "^1.2.2",
    "run-when-changed": "^2.1.0"
  }
}
